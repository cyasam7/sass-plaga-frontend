import { Container } from "@mui/material";

import { styled } from "@mui/material/styles";

export const StyledContainerClients = styled(Container)(({ theme }) => ({
  py: 4,
  maxWidth: "1400px !important",
}));
